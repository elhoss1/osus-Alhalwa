# متجر أُسُس الحلوى - Angular E-commerce Store

## نظرة عامة

متجر إلكتروني متكامل مبني باستخدام **Angular 19** مع تصميم حديث وجذاب، متكامل مع **WooCommerce API** من WordPress.

## المميزات

### 📱 الصفحات المتوفرة
- **الصفحة الرئيسية**: عرض المنتجات المميزة والفئات
- **جميع المنتجات**: عرض كافة المنتجات المتاحة
- **صفحة الكيك**: منتجات الكيك فقط
- **صفحة الشوكولاتة**: منتجات الشوكولاتة فقط
- **صفحة الحلويات**: منتجات الحلويات فقط
- **سلة التسوق**: إدارة المنتجات المضافة
- **صفحة الدفع**: إتمام عملية الشراء

### 🎨 التصميم
- تصميم عصري وجذاب بألوان متدرجة
- واجهة مستخدم سهلة الاستخدام
- تصميم متجاوب يعمل على جميع الأجهزة
- تأثيرات حركية سلسة
- دعم كامل للغة العربية (RTL)

### 🛠️ التقنيات المستخدمة
- **Angular 19** - إطار العمل الرئيسي
- **TypeScript** - لغة البرمجة
- **SCSS** - للتنسيقات
- **RxJS** - لإدارة البيانات التفاعلية
- **Angular Router** - للتنقل بين الصفحات
- **HttpClient** - للتواصل مع API

## 📋 المتطلبات

- Node.js (الإصدار 18 أو أحدث)
- npm أو yarn
- Angular CLI

## 🚀 التثبيت والتشغيل

### 1. تثبيت المتطلبات

```bash
npm install -g @angular/cli
```

### 2. تثبيت الحزم

```bash
cd osus-alhalwa-store
npm install
```

### 3. إعداد WooCommerce API

قم بتحديث ملف `src/app/services/woocommerce.ts` بمفاتيح API الخاصة بك:

```typescript
private consumerKey = 'YOUR_CONSUMER_KEY';
private consumerSecret = 'YOUR_CONSUMER_SECRET';
```

#### كيفية الحصول على مفاتيح API من WooCommerce:

1. سجل الدخول إلى لوحة تحكم WordPress
2. انتقل إلى: **WooCommerce** → **الإعدادات** → **متقدم** → **REST API**
3. انقر على **إضافة مفتاح**
4. املأ التفاصيل:
   - الوصف: `Angular Store`
   - المستخدم: اختر مستخدم إداري
   - الصلاحيات: **قراءة/كتابة**
5. انقر على **إنشاء مفتاح API**
6. انسخ **Consumer Key** و **Consumer Secret**

### 4. تشغيل المشروع في وضع التطوير

```bash
ng serve
```

افتح المتصفح على: `http://localhost:4200`

### 5. بناء المشروع للإنتاج

```bash
ng build --configuration production
```

الملفات المبنية ستكون في مجلد: `dist/osus-alhalwa-store/browser`

## 📁 هيكل المشروع

```
osus-alhalwa-store/
├── src/
│   ├── app/
│   │   ├── components/          # المكونات المشتركة
│   │   │   ├── header/         # رأس الصفحة
│   │   │   ├── footer/         # تذييل الصفحة
│   │   │   └── product-card/   # بطاقة المنتج
│   │   ├── pages/              # صفحات التطبيق
│   │   │   ├── home/           # الصفحة الرئيسية
│   │   │   ├── all-products/   # جميع المنتجات
│   │   │   ├── cake-products/  # منتجات الكيك
│   │   │   ├── chocolate-products/  # منتجات الشوكولاتة
│   │   │   ├── sweets-products/     # منتجات الحلويات
│   │   │   ├── cart/           # سلة التسوق
│   │   │   └── checkout/       # صفحة الدفع
│   │   ├── services/           # الخدمات
│   │   │   ├── woocommerce.ts  # خدمة WooCommerce API
│   │   │   └── cart.ts         # خدمة السلة
│   │   ├── app.ts              # المكون الرئيسي
│   │   ├── app.routes.ts       # مسارات التطبيق
│   │   └── app.config.ts       # إعدادات التطبيق
│   ├── styles.scss             # التنسيقات العامة
│   └── index.html              # الصفحة الرئيسية
├── angular.json                # إعدادات Angular
├── package.json                # حزم npm
└── tsconfig.json              # إعدادات TypeScript
```

## 🔧 الإعدادات المتقدمة

### تخصيص الألوان

يمكنك تعديل الألوان في ملف `src/styles.scss`:

```scss
// الألوان الرئيسية
$primary-color: #667eea;
$secondary-color: #764ba2;
$danger-color: #ff4757;
```

### إضافة فئات جديدة

1. أنشئ صفحة جديدة:
```bash
ng generate component pages/new-category
```

2. أضف المسار في `app.routes.ts`:
```typescript
{ path: 'products/new-category', component: NewCategoryComponent }
```

3. حدث القائمة في `header.html`

### تخصيص معرفات الفئات

في ملفات صفحات المنتجات (مثل `cake-products.ts`)، حدث معرف الفئة:

```typescript
// استبدل CAKE_CATEGORY_ID بالمعرف الفعلي من WooCommerce
this.woocommerceService.getProductsByCategory(CAKE_CATEGORY_ID, { per_page: 20 })
```

## 🔐 الأمان

⚠️ **مهم جداً**: لا تقم برفع مفاتيح API إلى GitHub أو أي مستودع عام!

استخدم متغيرات البيئة:

1. أنشئ ملف `src/environments/environment.ts`:
```typescript
export const environment = {
  production: false,
  woocommerce: {
    url: 'https://osus-alhalwa.com/wp-json/wc/v3',
    consumerKey: 'YOUR_KEY',
    consumerSecret: 'YOUR_SECRET'
  }
};
```

2. أضف الملف إلى `.gitignore`

## 📱 التوافق مع الأجهزة

المتجر متوافق مع:
- ✅ أجهزة الكمبيوتر المكتبية
- ✅ الأجهزة اللوحية
- ✅ الهواتف الذكية
- ✅ جميع المتصفحات الحديثة

## 🌐 النشر

### النشر على Vercel

```bash
npm install -g vercel
vercel
```

### النشر على Netlify

```bash
npm run build
# ارفع مجلد dist/osus-alhalwa-store/browser إلى Netlify
```

### النشر على خادم خاص

1. قم ببناء المشروع:
```bash
ng build --configuration production
```

2. ارفع محتويات `dist/osus-alhalwa-store/browser` إلى خادمك

3. تأكد من إعداد إعادة التوجيه للمسارات:
```
/* /index.html 200
```

## 📞 الدعم

للمساعدة أو الاستفسارات:
- البريد الإلكتروني: info@osus-alhalwa.com
- الموقع: https://osus-alhalwa.com

## 📄 الترخيص

هذا المشروع مرخص لـ **أُسُس الحلوى** - جميع الحقوق محفوظة © 2025

## 🙏 شكر وتقدير

تم بناء هذا المشروع باستخدام:
- Angular Framework
- WooCommerce REST API
- مكتبات مفتوحة المصدر

---

**ملاحظة**: هذا المشروع جاهز للاستخدام ويحتاج فقط إلى إضافة مفاتيح WooCommerce API الخاصة بك لبدء العمل.

للحصول على أفضل تجربة، تأكد من:
1. ✅ إضافة منتجات في WooCommerce
2. ✅ إنشاء الفئات المطلوبة (كيك، شوكولاتة، حلويات)
3. ✅ إضافة صور عالية الجودة للمنتجات
4. ✅ تفعيل CORS في WordPress إذا لزم الأمر

